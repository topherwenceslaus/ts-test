/**
 * Implement a Function that calculates the Fibonacci Sequence
 * for n numbers.
 */

const fib = (size: number): number[] => {
  // Replace with actual implementation here.

  return [-1];
};

export const result = fib(10);
